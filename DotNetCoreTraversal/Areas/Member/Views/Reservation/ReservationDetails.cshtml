@model Reservation
@{
    ViewData["Title"] = "DestinationDetails";
    Layout = "~/Views/Shared/_UserDashboardLayout.cshtml";
}

<style>
    img {
        height: 100px !important;
        width: auto !important;
    }
</style>

<div class="card">
    <h3 class="card-header">Rezervasyon Detayları</h3>
    <div class="card-body">
        <br />
        <div class="row h6 align-items-center">
            <div class="col-3">
                Rezervasyon Yapılan Şehir:
                <div class="mt-2">
                    <img src="@Model.Destination.Image" class="img-thumbnail rounded" /> &nbsp; <a href="/Destination/DestinationDetails/@Model.DestinationID">@Model.Destination.City.CityName</a>
                </div>
            </div>
            <div class="col-3 text-center">
                Rezervasyon Yapılma Tarihi:<br />
                <div class="text-bold text-primary">@Model.ReservationDate.ToString("dd.MM.yyyy [HH:mm]")</div>
            </div>
            <div class="col-3 text-center">
                Kişi Sayısı:<br />
                <div class="text-bold text-success">@Model.PersonCount</div>
            </div>
            <div class="col-3 text-center">
                Rezervasyon Durumu:<br />
                <div class="text-bold text-info">@Model.ReservationStat</div>
            </div>
        </div>
        <br />
        <div class="container-fluid">
            <h4>Notlar: </h4>
            <br />
            <p>@Model.Description</p>
        </div>
    </div>
</div>